#It is txt format please convert it in python extension format 

#first type cmd in terminal -- pip install chatterbot
#also type in terminal-- pip install chatterbot_corpus

from chatterbot import ChatBot
from chatterbot.trainers import ListTrainer
from tkinter import *
import pyttsx3 as pp

# pyttsx3
bot = ChatBot("My Bot")

convo = [
??? 'hello',
??? 'hi there !',
??? 'what is your name ?',
??? 'My name is Claude, I am bot', 
??? 'how are you ?',
??? 'I am doing great these days',
??? 'thank you',
??? 'In which city you live ?',
??? 'I live in system',
??? 'In which language you talk?',
??? ' I mostly talk in english',
??? ' are you human', 
??? 'I am bot, work for cloud counselage', 
??? 'what internships did cloud counselage provide?',
??? ' Cloud Counselage provide many internships such as Cloud computing, AI, digital marketing, Machine learning, Java and many more. More information Google it',?? 
??? 'what is cloud counselage?', 
??? 'Cloud Counselage believe in working ground-up to train and groom the youth of India into IT & Management, building capacities in terms of an online workforce PAN India to deliver projects globally' 

]

trainer = ListTrainer(bot)

# now training the bot with the help of trainer

trainer.train(convo)

# answer = bot.get_response("what is your name?")
# print(answer)

# print("Talk to bot ")
# while True:
#???? query = input()
#???? if query == 'exit':
#???????? break
#???? answer = bot.get_response(query)
#???? print("bot : ", answer)

main = Tk()

main.geometry("500x650")

main.title("My Chat bot")
img = PhotoImage(file="chatbot-data.png")

photoL = Label(main, image=img)

photoL.pack(pady=6)

def ask_from_bot():
??? query = textF.get()
??? answer_from_bot = bot.get_response(query)
??? msgs.insert(END, "you : " + query)
??? print(type(answer_from_bot))
??? msgs.insert(END, "bot : " + str(answer_from_bot))
??? speak(answer_from_bot)
??? textF.delete(0, END)
??? 

frame = Frame(main)

sc = Scrollbar(frame)
msgs = Listbox(frame, width=80, height=20, yscrollcommand=sc.set)

sc.pack(side=RIGHT, fill=Y)

msgs.pack(side=LEFT, fill=BOTH, pady=10)

frame.pack()

# creating text field

textF = Entry(main, font=("Verdana", 20))
textF.pack(fill=X, pady=10)

btn = Button(main, text="Ask from bot", font=("Verdana", 20), command=ask_from_bot)
btn.pack()

main.mainloop()


